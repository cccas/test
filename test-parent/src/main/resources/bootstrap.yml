# Tomcat 配置
server:
  name: test
  port: 8088
  undertow:
    buffer-size: 1024
  tomcat:
    max-threads: 200 #最大线程数
    min-spare-threads: 100 #最小活跃线程数
    max-connections: 1000 #在任何给定的时间服务器接受并处理的最大连接数
  servlet:
    context-path: /test

